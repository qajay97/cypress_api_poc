{
  "name": "booking_cypress_api",
  "version": "1.0.0",
  "description": "API Automation",
  "scripts": {
    "test": "npx cypress run",
    "pretest": "npm i"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "typescript": "^5.2.2"
  }
}
